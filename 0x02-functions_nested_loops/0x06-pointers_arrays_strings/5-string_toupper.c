#include "main.h"
/**
 * *string_toupper - changes all lowercase letters to upeercase
 *@c: string to change
 *Return: String change
 */

char *string_toupper(char *c)

{
  int i;

  for (i = 0; c[i] != '\0'; i++)
    {
      if (c[i] >= 'a' && c[])
    }

}
